# Internal Validation Scripts

This directory contains developer/internal validation scripts.

These scripts are retained for local experimentation and regression checks,
but are **not** stable public APIs and should not be presented as primary
usage paths for external users.

Notes:
- some scripts include machine-specific assumptions (for example hard-coded paths)
- behavior may depend on local chemistry software installation and environment variables

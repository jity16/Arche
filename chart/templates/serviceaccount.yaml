{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "arche.serviceAccountName" . }}
  labels:
    {{- include "arche.labels" . | nindent 4 }}
{{- end -}}
